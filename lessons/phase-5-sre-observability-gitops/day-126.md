# Day 126 — REVIEW (Days 121–125: SRE Principles → Alertmanager)

Phase 5 · review day, no new content.

---

## Cumulative quiz (10 questions, spanning Days 121–125)
1. Define SLI, SLO, and SLA in one sentence each.
2. If the project's SLO is 99.5% over 30 days, roughly how many minutes is
   the error budget?
3. What decision should a fully-spent error budget force on the team?
4. What's toil, and what's the SRE-book cap on how much time it should consume?
5. Why must a `rate()` window be at least 4x the scrape interval?
6. Write the PromQL shape for computing p99 latency from a histogram.
7. What's the difference between a Prometheus recording rule and an
   alerting rule?
8. What do USE and RED each stand for, and which layer (host vs service)
   does each fit?
9. What's the difference between Alertmanager grouping and inhibition?
10. What must every alert's annotations include, and why?

## Redo-weakest-lab
Pick whichever lab felt shakiest this week and redo it end-to-end without
looking at the day file:
- Day 122/123: stand up kube-prometheus-stack from scratch on a fresh kind
  cluster and get a custom app metric scraped.
- Day 124: rebuild the RED+USE dashboard from an empty Grafana instance.
- Day 125: reconfigure Alertmanager routing + one silence + one inhibition
  rule from memory.

## Project cleanup/commit
- Confirm `docs/slo.md`, the `PrometheusRule` manifests, the
  `dashboards/project.json` ConfigMap, and the two runbooks are all
  committed and the repo README references them.
- Tidy any scratch alert rules or test webhooks created for drills this
  week — remove test-only routes from the Alertmanager config before Day 127.

## NotebookLM 1–3–7–21 catch-up
Re-run quizzes for Days 125 (1-back), 123 (3-back), 119 (7-back — FinOps II),
and Day 105 (21-back — AWS observability review) this morning, ~10 min total.

## Day 127 preview
Logging stack I: EFK vs Loki tradeoffs, and deploying Loki+promtail on the
cluster — the second pillar of observability after metrics.

Related: [[README|devops-training]]
