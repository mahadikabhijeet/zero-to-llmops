# Day 147 — REVIEW (Days 141–146: Platform Engineering → Interview Prep)

Phase 5 · review day, no new content.

---

## Cumulative quiz (10 questions, spanning Days 141–146)
1. What three pillars make up Backstage?
2. What is a golden path, and what problem does it solve?
3. What's the difference between SAST and dependency (SCA) scanning?
4. What is an SBOM used for?
5. What's the difference between a `ConstraintTemplate` and a `Constraint`
   in Gatekeeper?
6. Why must a timeout be shorter than the calling path's SLO latency?
7. Name the three circuit breaker states.
8. What's the difference between time-to-mitigate and time-to-root-cause?
9. Why should SLIs be defined before naming tools in a "design monitoring
   for X" answer?
10. If burn rate is 6x on a 30-day SLO window, roughly how long until the
    budget is exhausted?

## Redo-weakest-lab
Pick whichever exercise felt shakiest this week and redo it without notes:
- Day 142: build a Backstage software template from scratch and scaffold
  a throwaway service with it.
- Day 143: reconfigure a Gatekeeper `ConstraintTemplate`/`Constraint` pair
  and prove it rejects a bad manifest.
- Day 144: re-implement the circuit breaker and reproduce open →
  half-open → closed under fault injection.
- Day 146: redo the weakest of the two "design monitoring for X" questions
  cold, with SLIs stated first.

## Project cleanup/commit
- Confirm the Day 145 game-day postmortem's action items are tracked
  (issue/todo) and at least one has been actioned before Day 150.
- Confirm `catalog-info.yaml`, `templates/`, the Gatekeeper manifests, and
  the reliability-pattern code from Day 144 are all committed.

## NotebookLM 1–3–7–21 catch-up
Re-run quizzes for Days 146 (1-back), 144 (3-back), 140 (7-back — third P5
review), and Day 126 (21-back — first P5 review) this morning, ~10 min total.

## Day 148 preview
Resume checkpoint #2 + portfolio polish: README audit, architecture
diagrams, pinned repos, LinkedIn rewrite — a facilitator-led session.

Related: [[README|devops-training]]
