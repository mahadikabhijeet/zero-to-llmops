# Day 140 — REVIEW (Days 134–139: GitOps Milestone → Autoscaling/PDBs)

Phase 5 · review day, no new content.

---

## Cumulative quiz (10 questions, spanning Days 134–139)
1. What do `prune` and `selfHeal` do in an ArgoCD sync policy?
2. What's the GitOps-native way to roll back a bad deploy?
3. What's the difference between `PERMISSIVE` and `STRICT` mTLS?
4. How does Istio's canary approach differ from a Kubernetes rolling update?
5. What are the five steps of the chaos engineering scientific method?
6. What should the pass/fail criterion for a chaos experiment be?
7. What's the difference between a load test and a stress test?
8. What is "saturation point" and how is it identified from a dashboard?
9. What's the core difference between cluster-autoscaler and Karpenter?
10. What does a PDB protect against, and what does it not protect against?

## Redo-weakest-lab
Pick whichever lab felt shakiest this week and redo it end-to-end without
looking at the day file:
- Day 136: rerun a canary ramp (5%→50%) with an injected fault and abort,
  from scratch.
- Day 137: rebuild a `PodChaos` and `NetworkChaos` experiment and interpret
  the dashboard/alert response.
- Day 138: rewrite a k6 stress test from memory and find the saturation
  point again.

## Project cleanup/commit
- Confirm the `d134-gitops` tag and `docs/milestone-d134.md` are accurate.
- Confirm `docs/chaos-experiments.md`, `docs/capacity.md`, and the PDB/
  autoscaling docs are all committed.
- Reset the canary `VirtualService` weights to 100% stable version if left
  mid-ramp from any drill this week.

## NotebookLM 1–3–7–21 catch-up
Re-run quizzes for Days 139 (1-back), 137 (3-back), 133 (7-back — second
P5 review), and Day 119 (21-back — Phase 4 review) this morning, ~10 min total.

## Day 141 preview
Platform engineering: the IDP concept, golden paths, and installing
Backstage to catalog the project.

Related: [[README|devops-training]]
