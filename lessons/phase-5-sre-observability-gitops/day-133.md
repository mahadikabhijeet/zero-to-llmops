# Day 133 — REVIEW (Days 127–132: Logging → GitOps Basics)

Phase 5 · review day, no new content.

---

## Cumulative quiz (10 questions, spanning Days 127–132)
1. Why did we choose Loki over EFK for this project?
2. What's the risk of using `request_id` as a Loki label instead of leaving
   it in the log line?
3. Write a LogQL query that filters JSON logs for `status_code >= 500`.
4. What's the minimum set of fields a structured log line should carry?
5. What's the relationship between a trace and a span?
6. What must a log line carry to correlate it with a specific Jaeger trace?
7. What does a burn rate of 14.4x mean, and why use short + long windows
   for a burn-rate alert?
8. What are the five sections of the postmortem template?
9. What's the core difference between push-based and pull-based (GitOps)
   deployment?
10. What's the app-of-apps pattern, and what problem does it solve?

## Redo-weakest-lab
Pick whichever lab felt shakiest this week and redo it end-to-end without
looking at the day file:
- Day 128/129: add structured logging with trace correlation to a
  different endpoint of the app from scratch.
- Day 130: recompute burn-rate thresholds and rebuild the multi-window
  alerts from the SLO doc alone.
- Day 132: tear down and reinstall ArgoCD, redeploy `project-app`, and
  rebuild the app-of-apps root from memory.

## Project cleanup/commit
- Confirm the `d130-instrumented` tag exists and `docs/milestone-d130.md`
  is accurate.
- Confirm `docs/postmortems/`, `docs/incident-severity.md`, and the
  `apps/` ArgoCD directory are all committed.
- Remove any test-only ArgoCD `Application` manifests created purely for
  drift-testing this week.

## NotebookLM 1–3–7–21 catch-up
Re-run quizzes for Days 132 (1-back), 130 (3-back), 126 (7-back — first P5
review), and Day 112 (21-back — Phase 4 review) this morning, ~10 min total.

## Day 134 preview
GitOps II: ★ the project fully managed by ArgoCD — sync policies,
auto-prune, a rollback demo, and the image-updater concept.

Related: [[README|devops-training]]
