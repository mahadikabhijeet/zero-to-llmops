# Day 168 — REVIEW

Phase 6 · covers Days 162–167 (RAG, agents/tool-use, LLM cost/perf, ★ anomaly
detection, AIOps/Alertmanager wiring, capstone spec).

---

## Cumulative quiz (10 questions, spanning the week)
1. What problem does RAG solve that a plain LLM call cannot (D162)?
2. Why must the same embedding model index and query in RAG (D162)?
3. What's the difference between what an agent "proposes" and what your code
   "executes" (D163)?
4. Why must untrusted text ingested by an agent be treated as data, not
   instructions (D163)?
5. What's the difference between exact-match and semantic caching (D164)?
6. What two levers matter most for CPU-only LLM serving cost (D164)?
7. Why must a time-series train/test split respect time order (D165)?
8. What does `IsolationForest`'s `contamination` parameter represent (D165)?
9. What integration path did we use to wire the anomaly model into Alertmanager,
   and why (D166)?
10. What is the capstone's scope decision, and why reuse existing infra (D167)?

## Redo-weakest-lab
Pick the roughest of: the RAG pipeline, the agent/tool-use loop, or the
anomaly-detection + Alertmanager wiring — redo end to end without notes, since
Day 174 depends directly on the anomaly pipeline working. If all went cleanly,
spend the time hardening the capstone spec instead: fill in any success-criteria
gaps found rereading Day 167's document.

## Project cleanup/commit
- Confirm `ml/rag/`, `ml/agent/`, `ml/anomaly/` are all committed, tested, and
  documented in the project README.
- Confirm the `AnomalyDetected` alert has fired at least once (real or drilled)
  and its runbook is committed.
- `capstone-spec.md` committed with all 8 success criteria and the day-by-day
  table filled in — this is the single most important artifact to have clean
  before Day 169 starts building against it.

## NotebookLM 1-3-7-21 catch-up
Re-take quizzes for Day 167 (1 back), Day 165 (3 back — the milestone, worth a
careful re-check), Day 161 (7 back — prior review), and Day 147 (21 back — Phase 5
final review). ~10 min; flag anything needing a live re-explain before the build
sprint starts.

Related: [[README|devops-training]]
