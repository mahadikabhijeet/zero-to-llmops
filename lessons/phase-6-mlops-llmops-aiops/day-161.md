# Day 161 — REVIEW

Phase 6 · covers Days 155–160 (model serving, Kubeflow/SageMaker overview, ML
CI/CD, model monitoring, LLM fundamentals, LLMOps I).

---

## Cumulative quiz (10 questions, spanning the week)
1. Why load a model once at startup instead of per-request (D155)?
2. What's the difference between `/health` and `/v1/predict` (D155)?
3. Name one DIY-to-managed-platform mapping and one signal favoring DIY (D156)?
4. What are the four stages of the ML CI/CD pipeline (D157)?
5. Why does Production promotion stay a human-approved step (D157)?
6. What's the difference between data drift and concept drift (D158)?
7. Why can a degraded model fail "silently" (D158)?
8. What is a token, and what happens when a request exceeds the context window (D159)?
9. Why does output length dominate LLM latency more than input length (D159)?
10. Why does this course use Ollama/llama.cpp instead of vLLM (D160)?

## Redo-weakest-lab
Pick the roughest of: FastAPI model serving + Helm deploy, the ML CI/CD gate
pipeline, model-server drift instrumentation, or the Ollama OpenAI-compatible
setup — redo it end to end without notes. If all went cleanly, extend the
drift-monitoring dashboard (D158) with a second Gauge tracking a different input
feature, to sharpen the pattern before RAG's more complex pipeline arrives.

## Project cleanup/commit
- Confirm the model-server Helm release, CI workflow (`ml-retrain.yml`), and
  `/metrics` instrumentation are all committed and the CI pipeline is green.
- Confirm `ollama` (containerized) runs cleanly with resource limits and the
  recorded latency baseline from Day 160 is saved somewhere retrievable (e.g.,
  `ml/llm-baseline.md`) — Day 164 needs it.
- `git status` / `dvc status` both clean; project README updated to mention the
  new `ml/` serving and LLM components.

## NotebookLM 1-3-7-21 catch-up
Re-take quizzes for Day 160 (1 back), Day 158 (3 back), Day 154 (7 back — prior
review), and Day 140 (21 back — Phase 5 mid-review). ~10 min; flag anything
needing a live re-explain.

Related: [[README|devops-training]]
