# Day 154 — REVIEW

Phase 6 · covers Days 148–153 (resume checkpoint #2, mock interviews, Phase 5
capstone, ML lifecycle, MLflow, DVC).

---

## Cumulative quiz (10 questions, spanning the week)
1. What are the five ML lifecycle stages and their DevOps analogues (D151)?
2. Why must a model never be evaluated on its own training data (D151)?
3. What's the difference between an MLflow run and an experiment (D152)?
4. What does promoting a model to a Model Registry "Staging" stage mean, and what's
   the container/Helm analogy (D152)?
5. Where did the real training dataset in this week's labs come from (D152)?
6. What problem does DVC solve that MLflow does not (D153)?
7. What's inside a `.dvc` pointer file, and why is it small (D153)?
8. What triggers `dvc repro` to re-run a pipeline stage (D153)?
9. What's the difference between `dvc add` and letting a `dvc.yaml` stage own an
   output file (D153)?
10. What was the D150 capstone's four-part description of the project platform?

## Redo-weakest-lab
Pick whichever of the three labs (hello-model, MLflow tracking/registry, DVC
pipeline) had the roughest run-through and redo it start to finish without notes.
If all three went cleanly, instead extend the DVC pipeline with a third stage
(`evaluate`) that reads `metrics.json` and fails the pipeline if accuracy drops
below a threshold — first taste of an ML CI gate (preview of Day 157).

## Project cleanup/commit
- Confirm `/opt/fresher_project/ml/` has: `train.py`, `predict.py`,
  `pull_metrics.py`, `dvc.yaml`, `dvc.lock`, `requirements.txt`, `.dvc/config`
  (local remote), `data/` and `mlruns/` correctly gitignored.
- `dvc push` and `git push` both clean (no uncommitted changes, `dvc status` and
  `git status` both quiet).
- Verify a fresh clone + `dvc pull` + `dvc repro` reproduces the pipeline end to end
  on a clean checkout — the real test of reproducibility discipline.

## NotebookLM 1-3-7-21 catch-up
Re-take quizzes for Day 153 (1 back), Day 151 (3 back), Day 147 (7 back — Phase 5
review), and Day 133 (21 back — GitOps I review). ~10 min total; note any topic
that needs a live re-explain in the next session.

Related: [[README|devops-training]]
