# Day 63 — REVIEW

Phase 3 · covers Days 57–62 (CI→deploy pipeline, secrets, mock interview,
Phase 2 capstone, k8s architecture, kubectl/Pods).

---

## Cumulative quiz (10 Qs — attempt closed-book, then check against day files)
1. Sketch the full pipeline: commit → ? → ? → deploy (Days 57, 60).
2. Where do secrets belong, and where must they NEVER go (Day 58)?
3. Name the 4 k8s control-plane components (Day 61).
4. What is etcd's role (Day 61)?
5. What does the declarative reconciliation loop do when a Pod dies under a
   Deployment vs a bare Pod (Day 61)?
6. `kubectl apply` vs `kubectl create` (Day 61)?
7. Init container vs sidecar container — order and purpose (Day 62)?
8. What do containers in the same Pod share (Day 62)?
9. Label vs annotation — which drives selection (Day 62)?
10. How do you view a crashed container's previous logs (Day 62)?

## Redo-weakest-lab
Pick whichever of Day 61's cluster-bring-up or Day 62's multi-container pod
felt shakiest; redo it from a fresh `kind create cluster` without looking at
the day file, then compare against it.

## Project checkpoint
Commit any lab manifests you want to keep as reference under
`/opt/fresher_project/k8s/examples/` (not yet part of the deployed app —
that starts Day 64+). Push to GitHub; confirm CI is still green from Phase 2.

## NotebookLM catch-up (1–3–7–21)
This morning, re-quiz Days 62 (1-back), 60 (3-back), 56 (7-back), and 42
(21-back), ~10 min total. Note any topic that fails twice — flag it for
extra review before Day 64 (Deployments).

Related: [[README|devops-training]]
