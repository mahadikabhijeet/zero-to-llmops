# Day 84 — REVIEW

Phase 3 · covers Days 78–83 (full-app Helm+CI milestone, Terraform I–IV,
IaC discipline).

---

## Cumulative quiz (10 Qs)
1. Why does `helm upgrade --install` matter for CI idempotency (Day 78)?
2. Name the 4 components bundled into the project's Helm release
   (Day 78)?
3. What's in a Terraform state file, and why must it never be committed
   (Day 79)?
4. Variable precedence: CLI `-var` vs `.tfvars` vs defaults (Day 80)?
5. `resource` vs `data` block — who owns what (Day 80)?
6. What problem does remote state + locking solve (Day 81)?
7. Does `terraform import` populate state, config, or both (Day 81)?
8. Why prefer `for_each` over `count` when items have identity (Day 82)?
9. What does `prevent_destroy` guard against (Day 82)?
10. State the provision-vs-configure boundary between Terraform and
    Ansible in one sentence (Day 83).

## Redo-weakest-lab
Pick whichever felt shakiest — the Day 78 CI-driven Helm deploy, Day 81's
workspaces/import, or Day 82's for_each refactor — and redo it from scratch
without the day file open.

## Project checkpoint
Confirm `terraform/` (modules, README documenting the provision/configure
boundary from Day 83) and the CI `terraform-ci.yml` workflow are committed
and green. Confirm the Day 78 Helm+CI milestone is still passing (rerun the
pipeline if it's been a few days). Push everything.

## NotebookLM catch-up (1–3–7–21)
This morning, re-quiz Days 83 (1-back), 81 (3-back), 77 (7-back), and 63
(21-back), ~10 min. Flag anything failing twice for review before the CKA
drill days (85–86).

Related: [[README|devops-training]]
