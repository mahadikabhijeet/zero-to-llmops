# Day 49 — REVIEW (Jenkins + Docker week)

Phase 2 · follows Day 48 (hardened image published to GHCR from CI).

---

## Cumulative quiz (10 Qs spanning Days 43–48)
1. What problem does `agent { docker { image ... } }` per-stage solve in Jenkins?
2. What's the security trade-off of mounting the Docker socket into a container?
3. How does Jenkins mask a credential's value in Console Output?
4. What's the difference between an image and a container?
5. Why should dependency-install steps come before source-copy steps in a Dockerfile?
6. What does a multi-stage build solve that a single-stage build doesn't?
7. Named volume vs bind mount — give one use case for each.
8. Why can't containers on the default bridge network resolve each other by name?
9. Why is running a container as root a real security risk?
10. What does `trivy image --exit-code 1 --severity HIGH,CRITICAL` do inside CI?

## Redo-weakest-lab instruction
Pick whichever felt shakiest and redo it end-to-end: the Jenkinsfile docker-agent setup
(Day 44), the cache-order Dockerfile experiment (Day 46), the Compose app+db network
lab (Day 47), or the non-root+healthcheck+trivy hardening pass (Day 48). If none felt
shaky, redo Day 48's full CI publish flow anyway — it's the most moving-parts day so
far this phase.

## Project cleanup / commit step
Confirm: `docker compose up` still works end-to-end, the CI pipeline's `test`→`scan`→
`publish` jobs are all green on `main`, the published GHCR image is the non-root
hardened version, and README documents how to pull/run it. Clean up any leftover
scratch containers/volumes/networks on your host (`docker system df`, `docker system
prune` — review what it'll remove before confirming). Commit and push any doc fixes.

## NotebookLM 1-3-7-21 catch-up
This morning, re-run recall quizzes for Day 48 (1-back), Day 46 (3-back), and Day 42
(7-back, the Python/GitHub Actions review). Spot-check one question each from Day 35
and Day 28 if time allows — Ansible starts fresh next week and doesn't lean on those,
but the habit matters more than the specific days.

Related: [[README|devops-training]]
