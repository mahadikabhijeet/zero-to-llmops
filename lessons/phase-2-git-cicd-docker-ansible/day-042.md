# Day 42 — REVIEW (Python-for-Ops, Data Formats, GitHub Actions week)

Phase 2 · follows Day 41 (matrix builds, secrets, caching, badges, CI milestone).

---

## Cumulative quiz (10 Qs spanning Days 36–41)
1. Why is `timeout=` mandatory on a `requests` call in automation code?
2. What does `raise_for_status()` do and why does checking `status_code` manually get
   forgotten?
3. Why mock `requests.get` in a pytest test instead of hitting the real network?
4. What's the standard `src/` + `tests/` layout supposed to solve?
5. Why does unquoted `NO` in YAML silently become a boolean?
6. What's the difference between `|` and `>` block scalars in YAML?
7. What does `jq -r` change about output, and what does `yq -i` do?
8. What's the difference between a `uses:` and a `run:` step in GitHub Actions?
9. What does a matrix build let you verify that a single-job workflow can't?
10. What does required-status-checks branch protection actually enforce on `main`?

## Redo-weakest-lab instruction
Pick whichever felt shakiest and redo it from scratch, retyping every command: the
retry-with-backoff function (Day 36), the mocked-`requests` pytest test (Day 38), the
jq/yq reshape drills (Day 39), or the branch-protection + matrix-CI setup (Day 41). If
none felt shaky, redo the branch-protection drill anyway — it's the one that gates every
future day's work.

## Project cleanup / commit step
In `/opt/fresher_project`: confirm `pytest` passes locally, the CI badge on GitHub is
green, `config.yaml` loads correctly, and `.github/workflows/ci.yml` has no leftover
debug steps (e.g., the demo-secret echo from Day 41 — remove it or keep it clearly
labeled as a demo). Commit and push any cleanup.

## NotebookLM 1-3-7-21 catch-up
This morning, re-run recall quizzes for Day 41 (1-back), Day 39 (3-back), and Day 35
(7-back, the Git review). Spot-check one question each from Day 21 and Day 14 if time
allows — both are past the 21-day window but cheap insurance before Jenkins/Docker
start next week.

Related: [[README|devops-training]]
