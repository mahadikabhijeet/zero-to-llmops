# Day 105 — REVIEW (S3, RDS/DynamoDB, DNS/CDN, Serverless, ECS, EKS)

Phase 4 · follows Day 104 (EKS + Helm deploy).

---

## Cumulative quiz (10 Qs spanning Days 99–104)
1. Why do deletes on a versioned S3 bucket become delete markers?
2. Why must RDS subnets span ≥2 AZs even for a single-AZ instance?
3. What's the access-control cascade for public S3 objects?
4. Why must a CloudFront-bound ACM cert be requested in `us-east-1`?
5. Push vs poll Lambda triggers — one example each?
6. Execution role vs task role on ECS — what does each control?
7. Why are ECS task definitions immutable, and what does that enable?
8. What's structurally different between EKS and a kind cluster, and what's identical?
9. What problem does IRSA solve at the pod level?
10. What's the FinOps use of an S3 lifecycle rule?

## Redo-weakest-lab
Pick the shakiest of Day 100 (RDS), 102 (serverless), 103 (ECS/Fargate), or 104
(EKS+IRSA) and redo the core flow from scratch, timed. If it's EKS, at minimum redo the
IRSA setup and Helm deploy without the cluster-creation wait time (script the cluster
bring-up so you don't lose the hour).

## Project cleanup / commit
- Confirm the project's Dockerfile, Helm chart, and CI workflow (ECR push + ECS/EKS
  deploy jobs) are committed and documented in the repo README.
- Sweep for stray billable resources: `aws ecs list-clusters`, `aws eks list-clusters`,
  `aws rds describe-db-instances`, `aws cloudfront list-distributions` — all should be
  empty given teardown discipline. Fix anything found.
- Tag the repo `v0.4-aws-deploys` once confirmed clean.

## NotebookLM 1–3–7–21 catch-up
Re-run quizzes for Days 104 (1-back), 102 (3-back), 98 (7-back review), and 84 (21-back).
Flag anything missed twice for a targeted re-read before Day 106.

**Definition of done:** quiz attempted and scored · weakest lab redone clean · AWS
console swept for stray resources and confirmed empty · repo tagged · catch-up quizzes
run.

## Day 106 preview
Observability on AWS: CloudWatch metrics/logs/alarms/dashboards, CloudTrail, and a
Config intro.

Related: [[README|devops-training]]
