# Day 91 — REVIEW (Phase 3 consolidation before cloud)

Phase 3 → Phase 4 boundary · follows Day 90 (★Phase 3 capstone: project on k8s via Helm,
kind cluster stood up by Terraform).

---

## Cumulative quiz (10 Qs spanning Days 84–90)
1. What's the difference between a Deployment rollout and a rollback, and which command
   triggers each?
2. Name the three Service types and one use case for each.
3. What does an Ingress controller do that a plain Service cannot?
4. PV vs PVC vs StorageClass — who requests what?
5. What do liveness, readiness, and startup probes each protect against?
6. What's in a RoleBinding vs a ClusterRoleBinding?
7. Taint vs toleration vs nodeAffinity — which one *repels* pods by default?
8. What does `helm upgrade --install` do differently from `helm install`?
9. Terraform: what problem does remote state + locking solve?
10. What's the boundary the course draws between Terraform's job and Ansible's job?

## Redo-weakest-lab
Pick the Day 84–90 lab you were least confident in live (etcd backup/restore, CRD
install, or the Helm+Terraform capstone are the usual suspects) and redo it from a clean
kind cluster, timed. If it's the capstone, focus only on the piece that broke.

## Project cleanup / commit
- `terraform validate && terraform plan` against the Day 90 kind-cluster module — confirm
  no drift.
- `helm lint` the project chart; `git status` — commit any uncommitted manifests/values.
- Tag the repo `v0.3-k8s-complete` (`git tag -a v0.3-k8s-complete -m "Phase 3 complete"`).
- Confirm README reflects current architecture (app+db+ingress+CronJob via own chart).

## NotebookLM 1–3–7–21 catch-up
Re-run quizzes for Days 90 (1-back), 88 (3-back), 84 (7-back), and 70 (21-back). Flag any
question missed twice in a row for a targeted re-read before Day 92.

**Definition of done:** quiz attempted (score noted) · weak lab redone clean · repo tagged
and pushed · catch-up quizzes run.

## Day 92 preview
Cloud concepts (IaaS/PaaS/SaaS, regions/AZs, shared responsibility), AWS free-tier
account creation, console tour, first IAM user — Phase 4 begins.

Related: [[README|devops-training]]
