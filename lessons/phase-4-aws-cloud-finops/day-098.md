# Day 98 — REVIEW (Cloud fundamentals, IAM, EC2, VPC)

Phase 4 · follows Day 97 (VPC II, bastion pattern).

---

## Cumulative quiz (10 Qs spanning Days 92–97)
1. What's the shared responsibility split for EC2 vs S3 vs Lambda?
2. Why must root never be used day-to-day, and what should it be reserved for?
3. Why do billing alerts have to exist before the first billable resource, and where
   are billing metrics published?
4. In IAM policy evaluation, what always wins — explicit Allow or explicit Deny?
5. Stopped vs terminated EC2 instance — what keeps billing, what doesn't?
6. What three things does an ASG need to launch instances?
7. What makes a subnet "public" in AWS's model?
8. Why is a NAT Gateway the most common surprise cost in a learning account, and what
   free alternative removes it for S3/DynamoDB traffic?
9. NACL vs SG — stateless vs stateful, and the ephemeral-port consequence?
10. Why is VPC peering non-transitive?

## Redo-weakest-lab
Pick whichever of Day 94 (EC2+SG), Day 95 (ASG/ALB), Day 96 (VPC+NAT), or Day 97
(bastion+NACL+endpoint) felt shakiest and redo it end-to-end from scratch, timed,
without referring back to the day file until you're stuck twice on the same step.

## Project cleanup / commit
- Confirm `docs/aws-account.md`, `docs/aws-cost/`, and `docs/aws-network/build-vpc.sh`
  are all committed and accurate.
- `aws ec2 describe-instances`, `describe-vpcs`, `elbv2 describe-load-balancers` — sweep
  for anything left running from this week's labs; terminate/delete if found.
- Check Cost Explorer and the Billing dashboard — confirm actual spend matches
  expectations (should still be near $0 given consistent teardown discipline).

## NotebookLM 1–3–7–21 catch-up
Re-run quizzes for Days 97 (1-back), 95 (3-back), 91 (7-back review), and 77 (21-back).
Flag anything missed twice for a targeted re-read before Day 99.

**Definition of done:** quiz attempted and scored · weakest lab redone clean · AWS
console swept clean of stray resources · docs committed · catch-up quizzes run.

## Day 99 preview
S3: buckets, storage classes, lifecycle policies, versioning, encryption, presigned
URLs, and hosting a static site.

Related: [[README|devops-training]]
