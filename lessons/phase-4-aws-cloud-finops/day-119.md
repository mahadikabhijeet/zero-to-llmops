# Day 119 — REVIEW (Azure/GCP mapping, multi-cloud reality, FinOps I & II)

Phase 4 · follows Day 118 (FinOps II: rightsizing, RI/Savings Plans/Spot, Kubecost).

---

## Cumulative quiz (10 Qs spanning Days 113–118)
1. What proved portable across EKS/AKS/GKE, and what didn't?
2. What's GKE Autopilot's differentiator versus standard node-group models?
3. Why is cross-cloud egress usually pricier than same-cloud cross-region transfer?
4. What's "data gravity" and why does it lock in cloud choice?
5. What are the three FinOps lifecycle phases?
6. Why is untagged spend effectively unallocatable spend?
7. Showback vs chargeback — what's the difference, and why start with showback?
8. What's the FinOps decision rule for on-demand vs commitments vs spot?
9. What are the two classic "silent leak" cost patterns in EBS/networking?
10. Why can't Cost Explorer alone attribute Kubernetes cost to a namespace or team, and
    what tool fills that gap?

## Redo-weakest-lab
Pick the shakiest of Day 114/115 (AKS/GKE Helm deploy), Day 117 (tagging + allocation),
or Day 118 (rightsizing + Kubecost) and redo its core flow from scratch, timed. If it's
Kubecost, at minimum redo the install and find one real right-sizing opportunity in the
project's chart values.

## Project cleanup / commit
- Confirm `docs/cloud-mapping/` (Days 113–116) and `docs/finops/` (Days 117–118) are
  fully committed, including the showback report template and rightsizing findings.
- Sweep for stray billable resources across all three clouds: `aws ec2
  describe-instances`, `az resource list`, `gcloud container clusters list` — all
  should be empty given teardown discipline.
- Tag the repo `v0.6-finops-complete` once confirmed clean.

## NotebookLM 1–3–7–21 catch-up
Re-run quizzes for Days 118 (1-back), 116 (3-back), 112 (7-back review), and 98
(21-back review). Flag anything missed twice for a targeted re-read before Day 120.

**Definition of done:** quiz attempted and scored · weakest lab redone clean · all
three clouds swept clean of stray resources · docs committed · repo tagged · catch-up
quizzes run.

## Day 120 preview
★Phase capstone: the project on AWS, cost-optimized, with a full FinOps report (spend,
tags, savings applied) plus a Terraform Associate/AWS SAA readiness check — closing out
Phase 4.

Related: [[README|devops-training]]
