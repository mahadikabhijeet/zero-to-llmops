# Day 112 — REVIEW (Observability, Terraform-on-AWS, WAR audit, end-to-end, SAA/resume)

Phase 4 · follows Day 111 (SAA drill + resume checkpoint #1).

---

## Cumulative quiz (10 Qs spanning Days 106–111)
1. Why does an average CPU metric hide problems a p99 metric would catch?
2. What does CloudTrail answer that CloudWatch Logs cannot?
3. Why can't a Terraform config create the S3 bucket it uses as its own backend?
4. What does `terraform state mv` let you do without destroying resources?
5. What's the difference between IAM Access Analyzer and Access Advisor?
6. Which two WAR pillars did the Day 109 audit focus on?
7. Why does infra get its own pipeline job separate from the app deploy job?
8. Why tag container images with the git SHA instead of `latest`?
9. What SAA scenario pattern maps to a VPC Gateway Endpoint?
10. What's the action+tech+outcome formula for a strong resume bullet?

## Redo-weakest-lab
Pick the shakiest of Day 107 (Terraform+remote state), 108 (module set), 109 (audit), or
110 (end-to-end pipeline) and redo its core flow from scratch, timed. If it's Day 110,
at minimum re-trigger the pipeline with a trivial change and confirm the smoke test
still catches a deliberately broken path.

## Project cleanup / commit
- Confirm the Day 110 environment's fate matches what was recorded in
  `docs/pipeline-e2e.md` (still live, or torn down) — if live, re-verify the smoke test
  passes today (drift check); if torn down, confirm via console sweep it's actually gone.
- Confirm `docs/aws-security/waraudit.md`, `terraform/README.md`, and the resume draft
  are all committed.
- Tag the repo `v0.5-aws-e2e` once confirmed clean/consistent.

## NotebookLM 1–3–7–21 catch-up
Re-run quizzes for Days 111 (1-back), 109 (3-back), 105 (7-back review), and 91
(21-back review). Flag anything missed twice for a targeted re-read before Day 113.

**Definition of done:** quiz attempted and scored · weakest lab redone clean ·
Day 110 environment state verified against docs · repo tagged · catch-up quizzes run.

## Day 113 preview
Azure mapping I: subscriptions/resource groups, VNet vs VPC, Entra ID vs IAM, VM/storage
equivalents — a concept-mapping table against everything already known in AWS.

Related: [[README|devops-training]]
