# Day 28 — REVIEW (Days 22–27)

Phase 1 · Week 4 review. No new content today: cumulative quiz, weak-spot lab redo,
project cleanup, NotebookLM catch-up.

---

## Cumulative quiz (10 questions, spanning Days 22–27)
1. Why is `"$var"` almost always safer than unquoted `$var` in bash?
2. What does `set -euo pipefail` add on top of default bash behavior, piece by piece?
3. What does shellcheck's SC2086 generally flag?
4. In `getopts "o:vh"`, what does the colon after `o` mean?
5. List vs tuple in Python — which is mutable?
6. What does `with open(...) as f:` guarantee over manual `open()`/`close()`?
7. Why prefer catching specific exceptions over a bare `except:`?
8. Why prefer a list of args over `shell=True` in `subprocess.run`?
9. What does `pathlib`'s `/` operator do?
10. What does `sys_report.py` currently capture (name all four sections)?

## Redo-weakest-lab instruction
Pick your least-confident lab from Days 22–27 — the `set -euo pipefail` refactor of
`boot_logger.sh` and the `sys_report.py` build are the two most involved labs this
week. Redo the weaker one cold, then diff against the day file and log the gap.

## Project cleanup / commit step
`/opt/fresher_project` now contains: robust-bash `boot_logger.sh` (passes shellcheck,
`-v` flag via getopts), `sys_report.py` (argparse-driven JSON snapshot tool), the
nginx-served `www/report.html`, and the rsync backup path to the second VM.
1. `sudo -u svc_fresher /opt/fresher_project/boot_logger.sh -v` and
   `sudo -u svc_fresher python3 /opt/fresher_project/sys_report.py -v` — both should
   run clean.
2. `shellcheck /opt/fresher_project/boot_logger.sh` — confirm still clean.
3. `~/lab17/backup_project.sh` — re-run, confirm delta transfer stays small even with
   the new `sys_report.py` file added.
4. `curl -I http://localhost/report.html` — confirm nginx still serves the latest HTML.
5. Update `~/lab-notes/week4-state.txt` with the current file inventory (this becomes
   the checklist for Day 30's capstone), then refresh the backup tarball:
   `tar czf ~/backups/fresher_project_day28.tar.gz -C /opt fresher_project`.

## NotebookLM 1–3–7–21 catch-up
Re-quiz **Day 27** (1 back), **Day 25** (3 back), **Day 21** (7 back — last review),
and **Day 7** (21 back). Load Days 22–27 into the Phase 1 notebook alongside Bash and
Python standard-library docs, and run: "Generate 10 exam-style questions on bash
scripting robustness and Python fundamentals (functions, files, exceptions, argparse,
subprocess), recall to troubleshooting difficulty. Don't show answers until I
respond."

**Definition of done:** quiz attempted honestly before checking answers · weakest lab
redone and gap logged · `boot_logger.sh` and `sys_report.py` both confirmed running
clean under `svc_fresher` · backup tarball refreshed · NotebookLM catch-up run across
1/3/7/21-day spacing.

Related: [[README|devops-training]] · [[knowledge-os/study-method]]
