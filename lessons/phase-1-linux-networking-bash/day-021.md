# Day 21 — REVIEW (Days 15–20)

Phase 1 · Week 3 review. No new content today: cumulative quiz, weak-spot lab redo,
project cleanup, NotebookLM catch-up.

---

## Cumulative quiz (10 questions, spanning Days 15–20)
1. What does `/24` mean and how many usable hosts does it give?
2. What's the difference between an nmcli device and a connection?
3. What does `dig @8.8.8.8 <domain>` help you isolate?
4. Why must you confirm SSH key login works before disabling password auth?
5. What's the difference between a runtime and permanent firewalld change?
6. When do you need a firewalld rich rule instead of `--add-service`?
7. DAC vs MAC — what's the conceptual difference?
8. Why doesn't `chcon` survive a relabel, unlike `semanage fcontext` + `restorecon`?
9. In `curl -v`, what do `>` and `<` lines mean?
10. Why does a self-signed TLS cert still encrypt traffic despite a trust warning?

## Redo-weakest-lab instruction
Pick your shakiest lab from Days 15–20 — subnetting math and the SELinux
fcontext/restorecon flow are the classic sticking points. Redo it cold, then diff
against the day file and log the exact gap.

## Project cleanup / commit step
By now `/opt/fresher_project` has: `boot_logger.sh` generating both text logs and
`www/report.html`, an rsync backup path to a second VM, and nginx serving the report
over HTTP and HTTPS with correct SELinux context.
1. `sudo -u svc_fresher /opt/fresher_project/boot_logger.sh` — confirm clean run,
   `www/report.html` freshens.
2. `curl -I http://localhost/report.html` and `curl -vk https://localhost/report.html`
   — both should 200.
3. `~/lab17/backup_project.sh` — re-run the rsync backup, confirm delta transfer is
   small (mostly unchanged files).
4. `firewall-cmd --list-all` and `getenforce` — confirm firewall/SELinux state matches
   what you intentionally configured, not accidental leftovers from lab experiments
   (e.g. confirm no stray rich rules or open test ports from Day 18 remain).
5. Update `~/lab-notes/week3-state.txt` with current project file layout, then refresh
   the backup tarball: `tar czf ~/backups/fresher_project_day21.tar.gz -C /opt
   fresher_project`.

## NotebookLM 1–3–7–21 catch-up
Re-quiz **Day 20** (1 back), **Day 18** (3 back), **Day 14** (7 back — last review),
and **Day 1** (roughly 21 back at this point in the course — good time to properly test
whether boot/systemd fundamentals have stuck). Load Days 15–20 into the Phase 1
notebook alongside networking/SELinux/nginx man pages and docs, and run: "Generate 10
exam-style questions on TCP/IP, SSH, firewalld, SELinux, and HTTP/TLS basics, recall to
troubleshooting difficulty. Don't show answers until I respond."

**Definition of done:** quiz attempted honestly before checking answers · weakest lab
redone and gap logged · nginx/HTTPS/rsync backup all confirmed working · firewall/
SELinux state audited for lab leftovers · backup tarball refreshed · NotebookLM 1-3-7-21
catch-up run including the Day 1 far-back check.

Related: [[README|devops-training]] · [[knowledge-os/study-method]]
