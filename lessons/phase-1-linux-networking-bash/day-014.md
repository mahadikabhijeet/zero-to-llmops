# Day 14 — REVIEW (Days 8–13)

Phase 1 · Week 2 review. No new content today: cumulative quiz, weak-spot lab redo,
project cleanup, NotebookLM catch-up.

---

## Cumulative quiz (10 questions, spanning Days 8–13)
1. SIGTERM vs SIGKILL — which can be trapped, and why does that matter for graceful
   shutdown?
2. What does `rpm -qf <path>` tell you, and when would you use it?
3. Where do repo definitions live, and what does `gpgcheck=1` protect against?
4. Why must `/etc/fstab` entries use UUID rather than `/dev/sdX`?
5. What are the three LVM layers, and in what order do you extend an LV vs its
   filesystem?
6. What's the difference between `Wants=` and `Requires=` in a systemd unit?
7. Why must you run `daemon-reload` after editing a unit file?
8. What's the difference between a systemd timer and cron, functionally?
9. `Storage=volatile` vs `Storage=persistent` in journald.conf — what changes?
10. What does `copytruncate` do in a logrotate config, and when is it required?

## Redo-weakest-lab instruction
Pick your least-confident lab from Days 8–13 — LVM extend-in-place and the systemd
timer/unit-file lab are the usual sticking points. Redo it cold (no day file open),
then diff against the file and note exactly where the gap was.

## Project cleanup / commit step
By now `/opt/fresher_project` should have: `boot_logger.sh` (no more hand-rolled
pruning — logrotate owns retention), a running `boot-logger.service` +
`boot-logger.timer` pair, and `/etc/logrotate.d/fresher_project` configured.
1. `systemctl status boot-logger.timer boot-logger.service` — confirm both healthy.
2. `journalctl -u boot-logger.service --since "1 hour ago" --no-pager` — confirm
   regular runs are landing in the journal.
3. `sudo logrotate -d /etc/logrotate.d/fresher_project` — dry-run confirms config
   still valid.
4. Update your baseline notes: `~/lab-notes/week2-state.txt` — list the two unit files
   and the logrotate config path, since Day 24 will refactor `boot_logger.sh` itself
   and Day 30 will build the full `system_report` tool around this same automation
   backbone.
5. Refresh the backup tarball: `tar czf ~/backups/fresher_project_day14.tar.gz -C /opt
   fresher_project`.

## NotebookLM 1–3–7–21 catch-up
Re-quiz **Day 13** (1 back), **Day 11** (3 back), **Day 7** (7 back — last review day),
and **Day 1** would be the 21-back slot once we're deeper into the course (not yet
applicable at Day 14). Load Days 8–13 into the Phase 1 notebook alongside the systemd/
journald/logrotate man pages, and run: "Generate 10 exam-style questions on processes,
package management, storage (LVM), and systemd/logging, recall to troubleshooting
difficulty. Don't show answers until I respond."

**Definition of done:** quiz attempted honestly before checking answers · weakest lab
redone and gap logged · `boot-logger.timer`/`.service` confirmed healthy, logrotate
config dry-run clean · backup tarball refreshed · NotebookLM catch-up run.

Related: [[README|devops-training]] · [[knowledge-os/study-method]]
